<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="WinUIToolkit.Examples.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="muxc:NavigationViewItemHeader">
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="muxc:NavigationViewItemHeader">
                            <Grid Padding="14,10,14,6"
                                  Background="{ThemeResource SubtleFillColorTertiaryBrush}"
                                  BorderBrush="{ThemeResource AccentFillColorDefaultBrush}"
                                  BorderThickness="3,0,0,0">
                                <TextBlock Text="{TemplateBinding Content}"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center"
                                           CharacterSpacing="80" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="FlyoutPresenter">
                <Setter Property="Background" Value="{ThemeResource SolidBackgroundFillColorBaseBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="CornerRadius" Value="8" />
            </Style>

            <SolidColorBrush x:Key="DatePickerFlyoutPresenterBackground" Color="{ThemeResource SolidBackgroundFillColorBase}" />
            <SolidColorBrush x:Key="DatePickerFlyoutPresenterBorderBrush" Color="{ThemeResource ControlStrokeColorDefault}" />
            <SolidColorBrush x:Key="TimePickerFlyoutPresenterBackground" Color="{ThemeResource SolidBackgroundFillColorBase}" />
            <SolidColorBrush x:Key="TimePickerFlyoutPresenterBorderBrush" Color="{ThemeResource ControlStrokeColorDefault}" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
